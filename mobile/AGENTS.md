# Expo HAS CHANGED

This project uses Expo SDK 54 (stable). Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

Do NOT upgrade to canary/beta SDKs — the app must stay compatible with the App Store version of Expo Go.

`react-native-worklets` is pinned to 0.5.1 — the exact version baked into Expo Go SDK 54's native binary. Do not bump it (newer JS versions crash at startup with "Exception in HostFunction"). When changing native deps, check versions against `expo/bundledNativeModules.json`.
